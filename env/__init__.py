"""Simulated environment: schema, data generation and knowledge base.

Not a virtualenv despite the directory name -- `.gitignore` excludes only the
virtualenv artifacts (`env/bin/`, `env/lib/`, `env/pyvenv.cfg`) while keeping the
project files here version-controlled.
"""
